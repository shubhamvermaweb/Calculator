# Define the tuple
numbers = (2, 5, 3, 5, 8, 5, 10)

# Number to count occurrences of
target_number = 5

# Count occurrences using the count() method
count = numbers.count(target_number)

# Print the result
print(f"The number {target_number} appears {count} times in the tuple.")
